import { NewContactModalDirective } from './new-contact-modal.directive';

describe('NewContactModalDirective', () => {
  it('should create an instance', () => {
    const directive = new NewContactModalDirective();
    expect(directive).toBeTruthy();
  });
});
